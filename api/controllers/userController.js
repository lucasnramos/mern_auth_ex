/**
 * Sets user token
 * @param { fastify Request} - request
 * @param { fastify Response } - response object
 * @access public
 */
export const authUser = async (fastify, options) => {
  console.log('auth user controller');

  res.status(200);
}

